spring:
  mustache:
    suffix: .mustache
    prefix: classpath:/templates/
    servlet:
      cache: false
    charset: UTF-8


server:
  port: 8081

logging:
  level:
    org.springframework.web: DEBUG
